language: csharp
sudo: false
env:
  global:
  - TARGET=Rebuild
  matrix:
  - CONFIGURATION=Debug
  - CONFIGURATION=Release
addons:
  apt:
    packages:
    - nuget
    - nunit-console
    - make
mono:
 - alpha
 - beta
 - weekly
 - latest
 - 4.4.0
 - 4.2.3
 - 4.0.5
matrix:
 allow_failures:
  - mono: weekly
  - mono: alpha
install:
  - make packages
script:
  - make test
